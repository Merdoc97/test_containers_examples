spring:
  kafka:
    consumer:
      auto-offset-reset: earliest
      bootstrap-servers: ${spring.embedded.kafka.brokers}
    producer:
      bootstrap-servers: ${spring.embedded.kafka.brokers}

kafka:
  topic:
    json: json.t
    people: people.t
    rpc: rpc.t
    rpc-response: rpc.response


