spring:
  kafka:
    consumer:
      auto-offset-reset: earliest
      bootstrap-servers: ${spring.embedded.kafka.brokers}
      group-id: rpc
    producer:
      bootstrap-servers: ${spring.embedded.kafka.brokers}


kafka:
  topic:
    rpc: rpc.request
    rpc-response: rpc.response


